import { GetFirstGenrePipe } from './get-first-genre.pipe';

describe('GetFirstGenrePipe', () => {
  it('create an instance', () => {
    const pipe = new GetFirstGenrePipe();
    expect(pipe).toBeTruthy();
  });
});
